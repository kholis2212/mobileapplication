name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  
  # Icons
  cupertino_icons: ^1.0.8
  
  # State Management
  provider: ^6.1.5+1
  flutter_riverpod: ^2.6.1          # Update ke versi sesuai modul
  
  # HTTP Client
  http: ^1.2.2                       # Sesuai modul
  dio: ^5.8.0+1                      # Update ke versi sesuai modul
  
  # Local Storage
  shared_preferences: ^2.5.3         # Tambahan untuk local storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true